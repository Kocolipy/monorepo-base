node_modules
dist
build
coverage
playwright-report
test-results
# Generated by the conditional gates and gitignored, but prettier walks the
# tree itself: without these, a mutation or E2E run leaves `format:check`
# red on its own output until the directories are deleted.
artifacts
test/e2e/.auth
.vite
.stryker-tmp
*.min.*
package-lock.json
pnpm-lock.yaml
yarn.lock
