@AGENTS.md

## graphify

Overrides the AGENTS.md graphify rule on updating the graph:

- Dispatch the `graphify-runner` agent to run `update .` instead of running it yourself. AGENTS.md governs when.
